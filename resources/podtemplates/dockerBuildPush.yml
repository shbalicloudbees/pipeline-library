kind: Pod
metadata:
  name: kaniko
spec:
  containers:
  - name: kaniko
    image: gcr.io/core-workshop/kaniko:0.1
    imagePullPolicy: Always
    command:
    - /busybox/cat
    tty: true
  serviceAccountName: cloud-run-sa
  securityContext:
    runAsUser: 0
