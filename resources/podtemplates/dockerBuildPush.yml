kind: Pod
metadata:
  name: kaniko
spec:
  containers:
  - name: kaniko
    image: gcr.io/core-workshop/kaniko:0.2
    imagePullPolicy: Always
    command:
    - /busybox/cat
    tty: true
  securityContext:
    runAsUser: 0
