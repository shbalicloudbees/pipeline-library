     kind: Pod
     metadata:
       name: kaniko
     spec:
       containers:
       - name: kaniko
         image: gcr.io/kaniko-project/executor:debug
         resources:
           requests:
             memory: "500m"
         command:
         - /busybox/sh
         tty: true
